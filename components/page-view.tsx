"use client";

import { useEffect } from "react";
// import { usePostHog } from "posthog-js/react";

export const PageView = ({ analyticsKey }: { analyticsKey: string }) => {
  // const posthog = usePostHog();

  // useEffect(() => {
  //   if (analyticsKey) {
  //     posthog?.capture("$pageview");
  //   }
  // }, [analyticsKey/* , posthog */]);

  return null;
};
